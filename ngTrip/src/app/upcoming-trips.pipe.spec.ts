import { UpcomingTripsPipe } from './upcoming-trips.pipe';

describe('UpcomingTripsPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingTripsPipe();
    expect(pipe).toBeTruthy();
  });
});
